function bearABCD_Enemy()
{
	var attackBearA = place_meeting(floor(x),floor(y),bearEnemyA);
	var attackBearB = place_meeting(floor(x),floor(y),bearEnemyB);
	var attackBearC = place_meeting(floor(x),floor(y),bearEnemyC);
	var attackBearD = place_meeting(floor(x),floor(y),bearEnemyD);
	
	if (loadPlayer = dudeModel)
	{
		if (attackBearA && sprite_index = Dude_Atk1 && bearEnemyA.isBearHit = false && bearEnemyA.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyA)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearA && sprite_index = Dude_Atk2 && bearEnemyA.isBearHit = false && bearEnemyA.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyA)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	
	
		if (attackBearB && sprite_index = Dude_Atk1 && bearEnemyB.isBearHit = false && bearEnemyB.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyB)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearB && sprite_index = Dude_Atk2 && bearEnemyB.isBearHit = false && bearEnemyB.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyB)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	
	
		if (attackBearC && sprite_index = Dude_Atk1 && bearEnemyC.isBearHit = false && bearEnemyC.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyC)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearC && sprite_index = Dude_Atk2 && bearEnemyC.isBearHit = false && bearEnemyC.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyC)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	


		if (attackBearD && sprite_index = Dude_Atk1 && bearEnemyD.isBearHit = false && bearEnemyD.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyD)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearD && sprite_index = Dude_Atk2 && bearEnemyD.isBearHit = false && bearEnemyD.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyD)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	}
	

//----------------------OWLET MONSTER----------------
	if (loadPlayer = owletModel)
	{
		if (attackBearA && sprite_index = Owlet_Atk1 && bearEnemyA.isBearHit = false && bearEnemyA.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyA)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearA && sprite_index = Owlet_Atk2 && bearEnemyA.isBearHit = false && bearEnemyA.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyA)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	
	
		if (attackBearB && sprite_index = Owlet_Atk1 && bearEnemyB.isBearHit = false && bearEnemyB.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyB)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearB && sprite_index = Owlet_Atk2 && bearEnemyB.isBearHit = false && bearEnemyB.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyB)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	
	
		if (attackBearC && sprite_index = Owlet_Atk1 && bearEnemyC.isBearHit = false && bearEnemyC.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyC)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearC && sprite_index = Owlet_Atk2 && bearEnemyC.isBearHit = false && bearEnemyC.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyC)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	


		if (attackBearD && sprite_index = Owlet_Atk1 && bearEnemyD.isBearHit = false && bearEnemyD.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyD)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearD && sprite_index = Owlet_Atk2 && bearEnemyD.isBearHit = false && bearEnemyD.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyD)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	}
	
	
//------------------PINK MONSTER---------------------------------------
	if (loadPlayer = pinkModel)
	{
		if (attackBearA && sprite_index = Pink_Atk1 && bearEnemyA.isBearHit = false && bearEnemyA.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyA)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearA && sprite_index = Pink_Atk2 && bearEnemyA.isBearHit = false && bearEnemyA.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyA)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	
	
		if (attackBearB && sprite_index = Pink_Atk1 && bearEnemyB.isBearHit = false && bearEnemyB.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyB)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearB && sprite_index = Pink_Atk2 && bearEnemyB.isBearHit = false && bearEnemyB.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyB)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	
	
		if (attackBearC && sprite_index = Pink_Atk1 && bearEnemyC.isBearHit = false && bearEnemyC.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyC)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearC && sprite_index = Pink_Atk2 && bearEnemyC.isBearHit = false && bearEnemyC.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyC)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	


		if (attackBearD && sprite_index = Pink_Atk1 && bearEnemyD.isBearHit = false && bearEnemyD.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyD)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
		else if (attackBearD && sprite_index = Pink_Atk2 && bearEnemyD.isBearHit = false && bearEnemyD.bearHP > 1)
		{
			global._Score += 100;
			audio_play_sound(hitSound,3,false);
			with (bearEnemyD)
			{
				isBearHit = true;
				bearHP -= MonsterPlayer.STR;
				instance_create_layer(x-15,y-80,"Show_Damage",P_Dmg_Display);
			}
		}
	}


	
	
}